PUERTO=3000

DB_HOST=localhost
DB_PORT=3306
DB_USUARIO=root
DB_PASSWORD=
DB_NOMBRE=empresa_vehiculos

# Generar uno propio con: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=cambiar-por-un-secreto-largo-y-aleatorio
JWT_EXPIRA=8h

# Impuesto de ventas (13% en Costa Rica)
IVA=0.13

# Origenes permitidos para el frontend, separados por coma
CORS_ORIGEN=http://localhost:5173
